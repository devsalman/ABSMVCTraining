delta DTypeModel(Int interestAttribute);
uses MAccountModel;
modifies interface Account
{
    adds Unit setInterest(Int interest);
    adds Int getInterest();
}

modifies class AccountImpl
{
    adds Int interest = interestAttribute;

    adds Unit setInterest(Int interest)
    {
        this.interest = interest;
    }

    adds Int getInterest()
    {
        return this.interest;
    }
}
