// Define your product(s) here.
product Default(
    Login,
    Account,
        Deposit,
        Withdraw
);

product IBWithWithdrawFee(
    Login,
    Account,
        Deposit,
        Withdraw,
            WithdrawFee
);

product IBWithCheckBalance(
    Login,
    Account,
        Deposit,
        Withdraw,
            CheckMinimumBalance
);

product IBWithContactPage(
    Login,
    Account,
        Deposit,
        Withdraw,
    ContactPage
);
